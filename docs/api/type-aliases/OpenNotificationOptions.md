[**@octopus-community/react-native v1.13.3**](../README.md)

---

[@octopus-community/react-native](../README.md) / OpenNotificationOptions

# Type Alias: OpenNotificationOptions

> **OpenNotificationOptions** = `Pick`\<[`OpenUIOptions`](../interfaces/OpenUIOptions.md), `"onBackRequested"`\>

Options `openNotification` shares with [openUI](../functions/openUI.md) — it presents the very same
fullscreen container, so the callback means exactly the same thing there.
